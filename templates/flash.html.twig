{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="p-3 bg-{{ label }} text-white container mt-5">
            <p>{{ message }}</p>
        </div>
    {% endfor %}
{% endfor %}
