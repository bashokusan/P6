{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="p-3 mb-2 bg-{{ label }} text-white">
            <p>{{ message }}</p>
        </div>
    {% endfor %}
{% endfor %}
